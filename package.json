{
  "name": "event-ping-bot",
  "version": "1.0.0",
  "description": "Event Ping - Discord bot for managing automatic event timers and role pings",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "register": "node src/deploy-commands.js",
    "build": "node --check src/index.js && node --check src/deploy-commands.js && node --check src/handlers.js && node --check src/scheduler.js && node --check src/commands.js && node --check src/embeds.js && node --check src/duration.js && node --check src/events.js && node --check src/db.js && echo \"Build successful\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "discord.js": "^14.16.2"
  }
}
